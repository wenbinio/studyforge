<Window x:Class="StudyForge.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudyForge"
        xmlns:views="clr-namespace:StudyForge.Views"
        mc:Ignorable="d"
        Title="StudyForge - Comprehensive Study App" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- Button Styles -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2C3E50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#34495E"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Left Sidebar Navigation -->
        <Border Grid.Column="0" Background="#2C3E50" Padding="10">
            <StackPanel>
                <TextBlock Text="StudyForge" FontSize="24" FontWeight="Bold" 
                          Foreground="White" Margin="10,20,10,30" HorizontalAlignment="Center"/>
                
                <Button Content="📊 Dashboard" Style="{StaticResource NavButtonStyle}"
                        Command="{Binding NavigateToDashboardCommand}"/>
                <Button Content="🗂️ Flashcards" Style="{StaticResource NavButtonStyle}"
                        Command="{Binding NavigateToFlashcardsCommand}"/>
                <Button Content="📝 Notes" Style="{StaticResource NavButtonStyle}"
                        Command="{Binding NavigateToNotesCommand}"/>
                <Button Content="⏱️ Pomodoro" Style="{StaticResource NavButtonStyle}"
                        Command="{Binding NavigateToPomodoroCommand}"/>
                <Button Content="⚙️ Settings" Style="{StaticResource NavButtonStyle}"
                        Command="{Binding NavigateToSettingsCommand}"/>
            </StackPanel>
        </Border>
        
        <!-- Main Content Area -->
        <Border Grid.Column="1" Background="#ECF0F1" Padding="20">
            <ContentControl>
                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Style.Triggers>
                            <!-- Dashboard View -->
                            <DataTrigger Binding="{Binding CurrentView}" Value="Dashboard">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <views:DashboardView/>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            
                            <!-- Flashcards View -->
                            <DataTrigger Binding="{Binding CurrentView}" Value="Flashcards">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <views:FlashcardsView/>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            
                            <!-- Notes View -->
                            <DataTrigger Binding="{Binding CurrentView}" Value="Notes">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <views:NotesView/>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            
                            <!-- Pomodoro View -->
                            <DataTrigger Binding="{Binding CurrentView}" Value="Pomodoro">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <views:PomodoroView/>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            
                            <!-- Settings View -->
                            <DataTrigger Binding="{Binding CurrentView}" Value="Settings">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <views:SettingsView/>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>
        </Border>
    </Grid>
</Window>
