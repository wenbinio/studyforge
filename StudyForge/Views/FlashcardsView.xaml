<UserControl x:Class="StudyForge.Views.FlashcardsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Flashcards" FontSize="32" FontWeight="Bold"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="➕ Create Card" Padding="15,10" Margin="5" 
                       Background="#2ECC71" Foreground="White" BorderThickness="0" 
                       FontSize="14" Cursor="Hand" Click="CreateCard_Click"/>
                <Button Content="📚 Start Review" Padding="15,10" Margin="5" 
                       Background="#3498DB" Foreground="White" BorderThickness="0" 
                       FontSize="14" Cursor="Hand" Click="StartReview_Click"/>
            </StackPanel>
        </Grid>
        
        <!-- Flashcard List -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Filter/Search -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="Category:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox Width="150" Margin="0,0,20,0">
                        <ComboBoxItem Content="All" IsSelected="True"/>
                        <ComboBoxItem Content="General"/>
                    </ComboBox>
                    
                    <TextBlock Text="Status:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox Width="150">
                        <ComboBoxItem Content="All" IsSelected="True"/>
                        <ComboBoxItem Content="Due Today"/>
                        <ComboBoxItem Content="Upcoming"/>
                    </ComboBox>
                </StackPanel>
                
                <!-- Card List -->
                <ListBox Grid.Row="1" x:Name="FlashcardList" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#F8F9FA" Padding="15" Margin="0,5" CornerRadius="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Question}" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding Answer}" FontSize="12" Foreground="Gray" Margin="0,5,0,0" TextWrapping="Wrap"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                            <Border Background="#3498DB" CornerRadius="3" Padding="5,2" Margin="0,0,5,0">
                                                <TextBlock Text="{Binding Category}" FontSize="10" Foreground="White"/>
                                            </Border>
                                            <TextBlock FontSize="10" Foreground="Gray">
                                                <Run Text="Due:"/>
                                                <Run Text="{Binding NextReviewDate, StringFormat='dd MMM yyyy'}"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>
                                    
                                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                                        <Button Content="✏️" Width="30" Height="30" Margin="5" 
                                               Background="#F39C12" Foreground="White" BorderThickness="0" 
                                               Cursor="Hand" Click="EditCard_Click" Tag="{Binding}"/>
                                        <Button Content="🗑️" Width="30" Height="30" Margin="5" 
                                               Background="#E74C3C" Foreground="White" BorderThickness="0" 
                                               Cursor="Hand" Click="DeleteCard_Click" Tag="{Binding}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
